# Future MCP Server

This folder is reserved for a future MCP server that wraps iiko operations.

Suggested future contents:

- `server.py` - MCP entrypoint
- `tools/` - MCP tools that call the iiko API
- `resources/` - optional read-only resources
- `schemas/` - request and response payload schemas
